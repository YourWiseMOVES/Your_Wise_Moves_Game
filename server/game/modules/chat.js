/**  Function Should:
*/

const pool = require('../../modules/pool');

//the structure for what a chat message will look like
const sampleMessage = {
}

const chat =  async (action, gameId, socket, config) => {
    try {
        
    }   
    catch (err) {
        console.log('Error in chat handler', err);
    }
}


module.exports = chat;